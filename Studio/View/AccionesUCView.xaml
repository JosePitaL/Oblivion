<UserControl x:Class="Studio.View.AccionesUCView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Studio.View"
             mc:Ignorable="d" 
             d:DesignHeight="25" d:DesignWidth="100">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="2*" />
        </Grid.ColumnDefinitions>
        <Button Grid.Column="0" HorizontalAlignment="Left" Background="Transparent" BorderThickness="0" Content="{Binding AccionButton}" />
        <TextBox Grid.Column="1" HorizontalContentAlignment="Left" Height="25" Text="{Binding AccionTextBox}" />
    </Grid>
</UserControl>
